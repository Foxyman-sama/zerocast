[package]
name = "zerocast_server"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocast_core = { path = "../zerocast_core" }
tokio = { workspace = true }
serde = { workspace = true }
gstreamer = "0.22.0"
glib = "0.19.0"
rand = "0.10.1"
serde_json = "1.0.149"
